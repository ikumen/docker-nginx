version: '3.7'
services:
  nginx:
    build: .
    image: nginx
    container_name: nginx
    restart: "no"
    volumes:
      - '/data/nginx/certs:/etc/nginx/certs'
    ports:
      - "80:80"
      - "443:443"

networks:
  default:
    external:
      name: ${NETWORK}
